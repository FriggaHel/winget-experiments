name: Build Package

on:
  push:

jobs:
  build-msix:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v3
      - name: Install deps
        run: |
          winget install msix-packaging-tool

